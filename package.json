{
  "name": "pagespeed-slack-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Monitor PageSpeed metrics and send alerts to Slack",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "check": "npm run build && npm start"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
